class Solution:
    def removeDuplicates(self, nums: List[int]) -> int:
        
        ans = 0
        for i in nums:
            print('i', i)
            print('n' , nums[ans])
            if i != nums[ans]:
                ans += 1
                nums[ans] = i
                
                
                
        return ans + 1
     
