class Solution:
    def groupAnagrams(self, strs: List[str]) -> List[List[str]]:
        
        dic = {}
        for str in strs:
            key = ''.join(sorted(str))
            print(key)
            if key in dic:
                
                dic.get(key).append(str)
            else:
                dic[key] = [str]
                print(dic[key])
        return dic.values()
